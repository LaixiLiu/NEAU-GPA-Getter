pub struct Student {
    // 学号
    pub id: String,
    // 姓名
    pub name: String,
    // 学院
    pub college: String,
    //专业
    pub major: String,
}

pub struct Record {
    // id
    pub id: i32,
    // term
    pub term: String,
    // gpa
    pub gpa: f64,
    // 学号
    pub sid: String,
}
